# 实验配置 - Exp2: B2-Standard-KD（通用蒸馏基线）
# Hinton 2015 经典KL蒸馏（Forward KL）
# 针对24GB显存优化配置 - 7B全量(bf16)教师模型

experiment:
  name: "exp02_standard_kd"
  description: "通用蒸馏基线：Hinton 2015经典KL蒸馏（7B全量教师）"
  seed: 42

model:
  # 教师模型: Qwen2.5-7B-Instruct (bf16全量)
  teacher:
    enabled: true
    name: "/home/nihao/Qwen2.5-7B-Instruct"
    quantization: "none"
    device_map: "auto"

  # 学生模型: Qwen2.5-1.5B-Instruct
  student:
    name: "/home/nihao/Qwen2.5-1.5B-Instruct"
    lora:
      r: 16
      lora_alpha: 32
      lora_dropout: 0.05
      target_modules: ["q_proj", "k_proj", "v_proj", "o_proj"]
      bias: "none"
      task_type: "CAUSAL_LM"

training:
  learning_rate: 1e-4
  batch_size: 4
  gradient_accumulation_steps: 32  # effective batch=128
  num_epochs: 3
  warmup_ratio: 0.1
  weight_decay: 0.01
  max_grad_norm: 1.0
  logging_steps: 10
  save_steps: 100
  eval_steps: 100
  max_length: 1024

distillation:
  enabled: true
  method: "standard_kd"
  temperature: 2.0
  alpha: 0.5  # L_KD = α × L_soft + (1-α) × L_hard

data:
  train_path: "../../data/splits/train.jsonl"
  dev_path: "../../data/splits/dev.jsonl"
  test_path: "../../data/splits/test.jsonl"
  max_length: 1024

output:
  checkpoint_dir: "checkpoints/"
  log_dir: "logs/"
  result_dir: "results/"
